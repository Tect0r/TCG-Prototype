dist
coverage
node_modules
package-lock.json
CLAUDE.md
results

# Per-machine agent settings. Git-ignored and not part of the codebase, so a
# developer's local file must not be able to fail the shared format gate.
.claude/settings.local.json

# Generated by `npm run content:build`. `content:check` compares it byte-for-byte
# against what the builder emits, so Prettier must not reformat it.
packages/card-data/src/data/generated

# Generated by `npm run audit:status`, and compared byte-for-byte by
# `scripts/lib/status-audit.test.ts` for the same reason.
docs/status-audit.md

# Prettier's markdown printer is not idempotent on this file: a bold span
# that wraps across a line break makes it recompute the continuation-line
# indent from its own prior output, so repeated --write passes grow that
# indent without bound instead of converging (reproduced in isolation on a
# minimal fixture with the same pattern; confirmed pre-existing at 74a801e,
# not introduced by any tranche's content). Restructuring every wrapped bold
# span in this large, append-only historical record to dodge the bug is not
# a proportionate fix; excluding the one file that hits it is.
docs/milestones/M08-ai-lab-and-player-meta.md
